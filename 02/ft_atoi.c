#include <stdlib.h>
#include <stdio.h>

int	ft_atoi(const char *str)
{
	int i = 0;
	while (str[i])
	{
//		if (str[i] == '\n' || str[i] == '\t' || str[i] == ' ')
		if (str[i] 47 && str[i] < 58)
		{
		}

	}
	return ();
}

int main()
{
	return (0);:wq

}
